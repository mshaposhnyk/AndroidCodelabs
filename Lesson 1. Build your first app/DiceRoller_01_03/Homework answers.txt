1. Which <ImageView> attribute indicates a source image that should be used only in Android Studio?
  -tools:src

2. Which method changes the image resource for an ImageView in Kotlin code?
  -setImageResource()

3. What does the lateinit keyword in a variable declaration indicate in Kotlin code?
   -The variable will be initialized later. I promise!

4. Which Gradle configuration indicates the most recent API level your app has been tested with?
   -targetSdkVersion

5. You see an import line in your code that starts with androidx. What does this mean?
   -The class is part of the Android Jetpack libraries.
